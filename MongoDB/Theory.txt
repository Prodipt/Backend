Data Storage:
Types of DBS - SQL | NoSQL

SQL - data ko ek roop mein rakhna - tables

NoSQL
Actual database is mongoDB and based on NoSql theory
It will store data as object based.

{
    name : 
}

what and why
terminologies - collections, documents, schema, keys, models

Collection
👉 A group of related documents (like a "table" in SQL).
Example: users collection holds all user data.

Document
👉 A single record inside a collection (like a "row" in SQL).
Written in JSON format.

Schema (Mongoose)
👉 A blueprint that defines the structure of documents in a collection.
Example: Every user must have name, age, and email.
 
Keys
👉 The field names inside a document (like name, age, email).

Model (Mongoose)
👉 A JavaScript object based on a schema that lets you create, read, update, and delete documents in a collection

Database -> Collection -> Document 

CODE
--------------------------

CODE                DATABASE
-----------------------------------
Mongoose.connect -> database create
model create     -> collection
CREATE           -> document


Mongodb installation
Mongodb collection
schema
model 
CRUD

different server k beech me baat krata h, Mongoose
schema - base users ki details