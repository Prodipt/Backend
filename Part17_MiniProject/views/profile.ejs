<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 px-3 py-2 text-sm mb-5 inline-block rounded-md" href="/logout">Logout</a>
        </div>
        <div class="flex items-start gap-4">
            <div class="w-15 h-15 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-3xl mb-5"><span class="font-light">Konnichiwa</span>, <%= user.name %>👋🏻</h3>
        </div>
        <h5 class="mb-5 mt-3">You can create a new post.</h5>
        <form method="post" action="/post">
            <textarea class=" block w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" placeholder="What's on your mind" name="content"></textarea>
            <input class="px-3 w-40 py-2 bg-blue-500 block rounded-md mt-2" type="submit" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer flex gap-4 flex-wrap">
                
                <% user.posts.reverse().forEach(function(post) { %>
                 <div class="post w-1/3 p-3 mb-4 rounded-md bg-zinc-800 border-[1px] border-zinc-700">
                    <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %></small>
                    <div class="btns flex gap-4 mt-1">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                    </div>
                </div> 
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>