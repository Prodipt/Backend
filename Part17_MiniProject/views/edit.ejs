<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 px-3 py-2 text-sm mb-5 inline-block rounded-md" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">Edit Your Post.</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea class=" block w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" placeholder="What's on your mind" name="content"><%= post.content %></textarea>
            <input class="px-3 w-40 py-2 bg-yellow-500 block text-black rounded-md mt-2" type="submit" value="Update Post">
        </form>
    
    </div>
</body>
</html>